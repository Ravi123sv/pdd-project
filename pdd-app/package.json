{
  "name": "neurosignal-enterprise",
  "version": "2.5.0",
  "private": true,
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "cd backend/express-server && npm start",
    "streamer": "cd backend/go-streamer && ./gin-server.exe",
    "launch": "concurrently \"npm run frontend\" \"npm run backend\" \"npm run streamer\"",
    "install:all": "npm install && cd frontend && npm install && cd ../backend/express-server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
